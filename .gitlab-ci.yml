include:
  - remote: http://gitlab.shopzilla.com/terraform/pipeline-templates/-/raw/master/1.5_testing_template.gitlab-ci.yml

stages:
  - init
  - validate
  - build
  - deploy
  - cleanup

build:
  extends: .build

deploy:
 extends: .deploy
 dependencies:
   - build